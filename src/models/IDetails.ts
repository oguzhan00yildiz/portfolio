export interface IDetails {
  about: string,
  introduction: string,
  gifs: string[]
  whatILearned: string,
  gifsFooter: string[],
  link: {
    title: string,
    url: string,
  };
  imageSrc: string;
}